<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:orientation="vertical"
    android:gravity="center">

    <!-- Instruction TextView -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Adjust the SeekBar to set the threshold for significant movement."
        android:textAlignment="center"
        android:paddingBottom="24dp"/>

    <!-- SeekBar for adjusting threshold -->
    <SeekBar
        android:id="@+id/thresholdSeekBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:max="100"
        android:progress="50"
        android:paddingBottom="16dp"/>

    <!-- TextView to display current threshold -->
    <TextView
        android:id="@+id/thresholdValueTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Current Threshold: 50"
        android:textAlignment="center"/>

</LinearLayout>